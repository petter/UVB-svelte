module.exports = {
    theme: {
        extend: {
            inset: {
                '1': '100%'
            }
        }
    },
    variants: {},
    plugins: [require('tailwindcss-transitions')()]
};
